MySQL Assignment -1 (DDL)

Q1.Login to MySQL and view all databases already present. You should get
     following result :

command:
  show databases
    -> ;
 create database db1;

show databases
    -> ;

+--------------------+
| Database           |
+--------------------+
| information_schema |
| db1                |
| mysql              |
+--------------------+

Q2.Write an SQL statement to create a simple table countries including columns
country_id,country_name and region_id. After this display the structure of
table as below :

command: 
  create table countries(create_id int(11), country_name varchar(20), region_id int(11));